# Discogs Collection Viewer
Express App that renders your collection and wantlist using the [Dicogs API](https://www.discogs.com/developers). Create for [my personal Vinyl collection](https://vinyl.lindeman.it), so do not expect a production ready application!

## Install
Make sure to add an `.env` file with tour Discogs credentials, e.g.:
```
DISCOGS_USER_TOKEN=<your-user-token>
DISCOGS_USERNAME=<your-user-name>
PORT=3012
DEBUG=vinyl:*
```

```bash
docker compose up --build
```
